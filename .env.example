# Leave blank to use the deterministic, offline demo provider.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini

# Optional PostgreSQL persistence. The demo runs in memory when omitted.
DATABASE_URL=postgres://broker:broker@localhost:5432/broker_ai
